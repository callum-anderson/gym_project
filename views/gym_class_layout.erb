<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Gym Project</title>
  </head>
  <body>
    <h2>Welcome to Ur Gym!</h2>
    <ul>
      <li><a href="/gym_member">Show all members</a></li>
      <li><a href="/gym_class">Show all classes</a></li>
      <li><a href="/gym_booking">Show all class bookings</a></li>
      <li><a href="/gym_member/new">Register new member</a></li>
      <li><a href="/gym_class/new">Add new class</a></li>
    </ul>

    <%= yield %>

    <hr style="margin: 2em 0; border-width: 2px;">

    <% @all_classes.each do |c| %>
      <form action="/update_class/<%= c.id %>" method="post">
        Class: <input name="name" value="<%= c.name %>" type="text"><br>
        Capacity: <input name="capacity" value="<%= c.capacity %>" type="number" min="1" max="200"><br>
        <input type="submit" value="Update">
      </form>
      <form action="/gym_class/<%= c.id %>/delete" method="post">
        <input hidden name="name" value="<%= c.name %>">
        <input hidden name="capacity" value="<%= c.capacity %>">
        <input type="submit" value="Delete">
      </form>
      <form action="/add_member_to_class" method="post">
        <input hidden name="name" value="<%= c.name %>">
        <input hidden name="capacity" value="<%= c.capacity %>">
        <input hidden name="id" value="<%= c.id %>">
        <select name="member">
          <% @all_members.each do |m| %>
          <option value="<%= m.id %>"><%= m.first_name %> <%= m.last_name %></option>
          <% end %>
        </select>
        <input type="submit" value="Add member to class">
      </form>
    <% end %>

  </body>
</html>
