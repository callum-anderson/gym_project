<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Gym Project</title>
  </head>
  <body>
    <h2>Welcome to Ur Gym!</h2>
    <ul>
      <li><a href="/gym_member">Show all members</a></li>
      <li><a href="/gym_class">Show all classes</a></li>
      <li><a href="/gym_booking">Show all class bookings</a></li>
      <li><a href="/gym_member/new">Register new member</a></li>
      <li><a href="/gym_class/new">Add new class</a></li>
    </ul>

    <%= yield %>

    <hr style="margin: 2em 0; border-width: 2px;">

<% @all_members.each do |m| %>
  <form action="/update_member/<%= m.id %>" method="post">
    First name: <input name="first_name" value="<%= m.first_name %>" type="text"><br>
    Last name: <input name="last_name" value="<%= m.last_name %>" type="text"><br>
    Age: <input name="age" value="<%= m.age %>" type="number" min="16" max="120"><br>
    Contact: <input name="contact_number" value="<%= m.contact_number %>" type="text"><br>
    Email: <input name="email" value="<%= m.email %>" type="email"><br>
    <input type="submit" value="Update">
  </form>
  <form action="/delete_member/<%= m.id %>" method="post">
    <input hidden name="first_name" value="<%= m.first_name %>">
    <input hidden name="last_name" value="<%= m.last_name %>">
    <input hidden name="age" value="<%= m.age %>">
    <input hidden name="contact_number" value="<%= m.contact_number %>">
    <input hidden name="email" value="<%= m.email %>">
    <input type="submit" value="Delete">
  </form>
  <form action="/add_to_class" method="post">
    <input hidden name="first_name" value="<%= m.first_name %>">
    <input hidden name="last_name" value="<%= m.last_name %>">
    <input hidden name="age" value="<%= m.age %>">
    <input hidden name="contact_number" value="<%= m.contact_number %>">
    <input hidden name="email" value="<%= m.email %>">
    <input hidden name="id" value="<%= m.id %>">
    <select name="gym_class">
      <% @all_classes.each do |c| %>
      <option value="<%= c.id %>"><%= c.name %></option>
      <% end %>
    </select>
    <input type="submit" value="Add to class">
  </form>
<% end %>

</body>
</html>
